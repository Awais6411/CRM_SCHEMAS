export interface _address {
  street: string;
  apt_or_suite: string;
  city: string;
  province: string;
  country: string;
  zip_code: string;
  address_type: string;
  primary_address: boolean;
  comment: string;
  user_id: String;
  contact_id: String;
}
