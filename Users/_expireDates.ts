export interface _expireDates {
  x_date: string;
  line_of_business: string;
  expiration_premium: string;
  term: string[];
  related_policy: string;
  x_date_description: string;
  contact_id: String;
}
